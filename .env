# Local development → use live backend (local backend not required)
VITE_API_URL=https://igsc-backend-gold.vercel.app/api/v1

# Option A: local backend running on :5000
# VITE_API_URL=http://localhost:5000/api/v1

# Option B: Vite/Vercel same-origin proxy (/api → backend)
# VITE_API_URL=/api/v1
