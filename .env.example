# Production (Vercel): keep same-origin proxy
# VITE_API_URL=/api/v1

# Local development (recommended when local backend is not running)
VITE_API_URL=https://igsc-backend-gold.vercel.app/api/v1

# Local backend on port 5000
# VITE_API_URL=http://localhost:5000/api/v1
