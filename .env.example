# Same-origin path. Vercel proxies /api to the backend (see vercel.json),
# and vite.config.ts proxies it to localhost:5000 during development.
VITE_API_URL=/api/v1

# To call the backend directly instead of through the proxy:
# VITE_API_URL=https://igsc-backend-gold.vercel.app/api/v1
